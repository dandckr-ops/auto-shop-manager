# App email settings. Copy to .env on the sandbox when you are ready to send mail.

POSTGRES_DB=autoshop
POSTGRES_USER=autoshop
POSTGRES_PASSWORD=

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_TLS=true
